# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. In C++ a lambda expands around each center.

## Run

    make && ./longestpalindromesubstring
