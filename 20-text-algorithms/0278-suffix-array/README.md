# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. In C++ `std::string::compare` orders suffixes without copying them.

## Run

    make && ./suffixarray
