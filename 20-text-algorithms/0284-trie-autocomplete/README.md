# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. In C++ a `std::map`-of-`std::unique_ptr` trie yields lexicographic DFS for free.

## Run

    make && ./trieautocomplete
