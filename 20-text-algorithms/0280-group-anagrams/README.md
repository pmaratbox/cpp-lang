# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. In C++ a `std::unordered_set` of sorted-letter keys counts distinct classes.

## Run

    make && ./groupanagrams
