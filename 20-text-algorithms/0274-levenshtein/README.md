# 0274 — Levenshtein Distance

Compute the edit distance between "kitten" and "sitting", printing `3`. In C++ two rolling `std::vector<int>` rows keep the DP compact.

## Run

    make && ./levenshtein
