# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. In C++ a `std::array<int,256>` holds each byte's last position.

## Run

    make && ./boyermooresearch
