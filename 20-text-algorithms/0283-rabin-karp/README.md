# 0283 — Rabin-Karp Search

Use rolling-hash Rabin-Karp to find all start indices of "ab" in "xabxab", printing `1 4`. In C++ `unsigned long long` arithmetic keeps the modular rolling hash exact.

## Run

    make && ./rabinkarp
