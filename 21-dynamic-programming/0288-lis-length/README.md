# 0288 — Longest Increasing Subsequence

Compute the LIS length of [10,9,2,5,3,7,101,18], printing `4`. Patience sorting via `std::lower_bound` gives an O(n log n) solution.

## Run

    make && ./lislength
