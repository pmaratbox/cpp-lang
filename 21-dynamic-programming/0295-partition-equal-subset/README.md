# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. The problem reduces to a subset-sum DP for half the total via `std::accumulate`.

## Run

    make && ./partitionequalsubset
