# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. A `std::vector<int>` DP table filled bottom-up keeps the code idiomatic.

## Run

    make && ./coinchangemin
