# 0293 — Longest Palindromic Subsequence

Compute the longest palindromic subsequence length of "bbbab", printing `4`. A 2-D `std::vector` interval DP fills shorter substrings first.

## Run

    make && ./longestpalindromicsubseq
