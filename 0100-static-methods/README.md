# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. A `static` member function belongs to the type and is called as `MathUtil::square(...)` without an instance.

## Run

    make && ./staticmethods
