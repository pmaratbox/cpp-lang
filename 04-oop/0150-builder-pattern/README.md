# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. Each setter returns `*this` by reference so calls chain fluently before `build()`.

## Run

    make && ./pattern
