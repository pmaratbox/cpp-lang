# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. In C++ runtime polymorphism uses `virtual` methods through base-class pointers held in a `std::vector<std::unique_ptr<Shape>>`.

## Run

    make && ./virtualdispatch
