# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. In C++ the idiomatic stringification is an `operator<<` overload for `std::ostream`.

## Run

    make && ./override
