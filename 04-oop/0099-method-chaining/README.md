# 0099 — Method Chaining

Build a calculator that supports method chaining — start at `5`, then `.add(3).multiply(2)` — and print the result: `16`. Each method returns `*this` by reference, so the chained calls mutate and return the same object.

## Run

    make && ./chaining
