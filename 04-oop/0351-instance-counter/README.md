# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. A `static inline int count` is bumped in the constructor and shared across all instances.

## Run

    make && ./instancecounter
