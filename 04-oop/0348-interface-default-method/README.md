# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. C++ models a default method as a non-pure `virtual` with a body that a subclass may `override`.

## Run

    make && ./interfacedefaultmethod
