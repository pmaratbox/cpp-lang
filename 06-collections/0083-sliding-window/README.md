# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. The outer loop advances the start index while the window fits (C++23 adds `std::views::slide` for this).

## Run

    make && ./window
