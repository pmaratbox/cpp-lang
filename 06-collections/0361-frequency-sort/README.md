# 0361 — Frequency Sort

Sort [1,1,2,3,3,3] by descending frequency (ties keep first-seen order), printing `3 3 3 1 1 2`. Count with a map and stable_sort the first-seen order by count descending.

## Run

    make && ./frequencysort
