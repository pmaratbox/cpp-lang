# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. Put the removal set in a `std::set` and skip any element it `count`s, preserving the first list's order.

## Run

    make && ./difference
