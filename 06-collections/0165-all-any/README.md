# 0165 — All and Any

For [2,4,6] report whether all are even (yes) and whether any are odd (no), printing `yes no`. `std::all_of` and `std::any_of` from `<algorithm>` express these predicates directly.

## Run

    make && ./any
