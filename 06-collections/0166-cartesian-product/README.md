# 0166 — Cartesian Product

Print the cartesian product of [1,2] and ["a","b"] as `1a 1b 2a 2b`. Nested range-for loops with the first list outer produce pairs in the expected order.

## Run

    make && ./product
