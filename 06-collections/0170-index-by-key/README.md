# 0170 — Index By Key

Index people [(1,alice),(2,bob)] by id into a map, look up id 2, and print `id 2: bob`. Build a `std::map<int,std::string>` keyed by id, then `at(2)` retrieves the name.

## Run

    make && ./bykey
