# 0084 — Partition

Partition `1, 2, 3, 4, 5, 6` into evens and odds, printing `evens: 2 4 6` and `odds: 1 3 5`. A conditional expression picks which vector to `push_back` into (`std::partition` rearranges in place but does not split into two).

## Run

    make && ./partition
