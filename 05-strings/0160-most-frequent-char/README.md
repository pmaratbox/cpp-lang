# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. A `std::map` tallies each character, then a left-to-right scan picks the first one reaching the highest count.

## Run

    make && ./frequentchar
