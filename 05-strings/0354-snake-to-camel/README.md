# 0354 — Snake to Camel Case

Convert "hello_world" to camelCase `helloWorld`. Walking the string and capitalizing the char after each underscore avoids extra allocations.

## Run

    make && ./snaketocamel
