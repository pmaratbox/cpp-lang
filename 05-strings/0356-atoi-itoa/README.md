# 0356 — String<->Int Round Trip

Parse "-42" to the integer -42 and format it back to "-42", printing `-42 -42`. Hand-rolled atoi and itoa accumulate and extract digits without the standard conversions.

## Run

    make && ./atoiitoa
