# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. `std::ostringstream` accumulates with `<<` and yields the result via `.str()` (appending to a `std::string` also works).

## Run

    make && ./stringbuilder
