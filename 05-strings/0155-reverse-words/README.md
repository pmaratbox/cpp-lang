# 0155 — Reverse Words

Reverse the order of words in "hello world", printing `world hello`. An `std::istringstream` splits on whitespace and the words are emitted back-to-front.

## Run

    make && ./words
