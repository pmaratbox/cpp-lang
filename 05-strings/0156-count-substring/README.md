# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. Repeated calls to `std::string::find` advance past each match by the needle's length.

## Run

    make && ./substring
