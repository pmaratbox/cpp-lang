# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. A `start` flag tracks word boundaries; `std::toupper` capitalizes the first letter of each word through the `char&` reference.

## Run

    make && ./titlecase
