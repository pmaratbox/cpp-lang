# 0157 — Longest Common Prefix

Find the longest common prefix of ["flower","flow","flight"], printing `fl`. The prefix is whittled down with `substr` as each word reveals the first mismatching column.

## Run

    make && ./commonprefix
