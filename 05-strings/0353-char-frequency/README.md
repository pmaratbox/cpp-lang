# 0353 — Character Frequency

Count character frequencies in "aab" and print them in first-seen order `a:2 b:1`. A vector preserves first-appearance order while an unordered_map tallies counts.

## Run

    make && ./charfrequency
