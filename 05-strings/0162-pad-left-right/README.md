# 0162 — Pad Left and Right

Left-pad and right-pad "5" with spaces to width 3 and print both joined by a pipe: `  5|5  `. The `std::format` align specifiers `{:>3}` and `{:<3}` produce the right- and left-justified fields.

## Run

    make && ./leftright
