# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. `std::reverse` flips the `std::string`'s `char`s in place — fine for ASCII, but byte-wise like C, so multi-byte UTF-8 would break.

## Run

    make && ./reverse
