# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `vowels.find(ch)` returns `npos` when the character is not a vowel; any other result counts as a match.

## Run

    make && ./vowels
