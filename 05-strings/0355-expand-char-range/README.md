# 0355 — Expand Character Range

Expand the range spec "a-e" into the characters `abcde`. Incrementing a `char` from the start to the end code point builds the string directly.

## Run

    make && ./expandcharrange
