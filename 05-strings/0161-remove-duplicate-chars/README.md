# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. A `std::set` records seen characters and `insert().second` reveals whether each one is new.

## Run

    make && ./duplicatechars
