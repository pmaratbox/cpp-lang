# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `std::string::find` returns the index of the first match, or `std::string::npos` if the substring is absent.

## Run

    make && ./substring
