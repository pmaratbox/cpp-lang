# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. With equal lengths, `b` is a rotation of `a` exactly when `find` locates it inside `a + a`.

## Run

    make && ./rotation
