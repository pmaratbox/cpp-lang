# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. The `std::format` facility fills positional `{}` placeholders from the argument list.

## Run

    make && ./interpolation
