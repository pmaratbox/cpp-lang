# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. C++23 lacks stable runtime reflection, so the field names are declared explicitly in declaration order.

## Run

    make && ./fieldnames
