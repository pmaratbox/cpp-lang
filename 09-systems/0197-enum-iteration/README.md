# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. C++ enums are not reflectable, so we list the values in an `std::array` and map each to its name with a `switch`.

## Run

    make && ./iteration
