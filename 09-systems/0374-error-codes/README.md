# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. A `std::map<int, std::string>` provides the code-to-message lookup.

## Run

    make && ./errorcodes
