# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. The same branch runs twice via a small helper that takes the flag.

## Run

    make && ./featureflag
