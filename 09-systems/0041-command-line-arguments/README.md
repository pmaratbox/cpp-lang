# 0041 — Command-line Arguments

Read the first command-line argument and greet it, so running with `Ada` prints `hello, Ada`. Identical to C — `main`'s `argv[1]` is the first argument after the program name. Higher-level parsing usually pulls in a third-party library.

## Run

    make && ./args Ada
