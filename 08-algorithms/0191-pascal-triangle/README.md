# 0191 — Pascal Triangle

Print the first 4 rows of Pascal’s triangle, each row space-separated. Builds each next row in a `std::vector<int>` as sums of adjacent entries above.

## Run

    make && ./triangle
