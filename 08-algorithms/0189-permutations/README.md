# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Uses `std::next_permutation`, which yields lexicographic order from a sorted start.

## Run

    make && ./permutations
