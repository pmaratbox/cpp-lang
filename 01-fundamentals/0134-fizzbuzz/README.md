# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. A single loop with modulo checks and `std::cout` is the idiomatic C++ approach.

## Run

    make && ./fizzbuzz
