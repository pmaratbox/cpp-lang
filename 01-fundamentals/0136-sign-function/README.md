# 0136 — Sign Function

Print the sign (-1, 0, or 1) of -5, 0, and 5: `-1 0 1`. The branchless `(x > 0) - (x < 0)` idiom yields -1/0/1 directly from comparison results.

## Run

    make && ./function
