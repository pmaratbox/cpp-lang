# 0333 — Classify Even/Odd

Classify each of [1,2,3,4] as odd or even, printing `odd even odd even`. In C++ a ternary on `n % 2 == 0` selects the label cleanly.

## Run

    make && ./evenoddclassify
