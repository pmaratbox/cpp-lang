# 0138 — Boolean Truth Table

For each (a,b) in (T,T),(T,F),(F,T),(F,F) print a, b, and a AND b, a OR b, a XOR b as lowercase booleans, one row per line. The `std::boolalpha` manipulator prints bools as `true`/`false`, with `!=` standing in for XOR.

## Run

    make && ./truthtable
