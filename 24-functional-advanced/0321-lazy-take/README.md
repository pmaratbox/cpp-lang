# 0321 — Lazy Take

Build a lazy sequence of the natural numbers and take the first five, printing `1 2 3 4 5`. A mutable lambda with captured state acts as a generator that yields the next natural on each call.

## Run

    make && ./lazytake
