# 0327 — FlatMap

FlatMap [1,2,3] with x -> [x, x*10] and print the flattened result `1 10 2 20 3 30`. Each element expands into a small list and the inner results are appended into one flat vector.

## Run

    make && ./flatmap
