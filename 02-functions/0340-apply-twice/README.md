# 0340 — Apply Twice

Write applyTwice(f,x)=f(f(x)) and apply it with inc to 3, printing `5`. A function template takes the callable `f` so any lambda can be passed.

## Run

    make && ./applytwice
