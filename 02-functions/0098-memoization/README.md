# 0098 — Memoization

Compute `fibonacci(10)` recursively with memoization (caching each result so it is computed once) and print it: `55`. An `unordered_map` caches each result; `find` returns a hit before any recomputation.

## Run

    make && ./memo
