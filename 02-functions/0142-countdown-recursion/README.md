# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. The recursion prints each value and a separating space only before the next call, avoiding a trailing space.

## Run

    make && ./recursion
