# 0006 — Functions

Define `add(a, b)` returning their sum, call it with `3` and `4`, and print
the result. Same definition-before-use rule as C. C++ adds extras on top:
default arguments, function overloading (same name, different parameter
types), and templates for generic functions.

## Run

    make && ./functions
