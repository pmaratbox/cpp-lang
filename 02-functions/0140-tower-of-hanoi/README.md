# 0140 — Tower of Hanoi

Count the moves needed to solve Tower of Hanoi for 3 disks recursively and print `7`. A small recursive function `moves(n) = 2*moves(n-1) + 1` captures the count idiomatically in C++.

## Run

    make && ./ofhanoi
