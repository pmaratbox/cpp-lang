# 0139 — Recursive Power

Compute 2 raised to the 10th power recursively and print `1024`. In C++ a plain recursive function with an `exp == 0` base case expresses this directly.

## Run

    make && ./power
