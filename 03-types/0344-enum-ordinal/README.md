# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. A scoped `enum class` casts to its underlying int for the ordinal, paired with a name table.

## Run

    make && ./enumordinal
