# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. In C++ the modern `using Meters = int;` alias is preferred over `typedef`.

## Run

    make && ./alias
