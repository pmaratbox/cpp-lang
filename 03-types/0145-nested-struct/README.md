# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Aggregate initialization fills nested structs positionally with brace-enclosed lists.

## Run

    make && ./struct
