# 0343 — Optional Chaining

Read a deeply optional value a.b.c when present (5) and when absent (default 0), printing `5 0`. C++ uses `std::optional` with explicit `&&` guards before the fallback.

## Run

    make && ./optionalchaining
