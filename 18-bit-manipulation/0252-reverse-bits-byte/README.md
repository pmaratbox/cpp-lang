# 0252 — Reverse Bits in a Byte

Reverse the 8 bits of the value 1 (00000001) to get 128 (10000000), printing `128`. In cpp a `std::uint8_t` loop shifts bits out of one end and into the other.

## Run

    make && ./reversebitsbyte
