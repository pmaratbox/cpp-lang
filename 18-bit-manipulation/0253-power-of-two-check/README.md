# 0253 — Power of Two Check

Test whether 16 (yes) and 18 (no) are powers of two, printing `yes no`. In cpp the classic `n > 0 && (n & (n - 1)) == 0` test does it without loops.

## Run

    make && ./poweroftwocheck
