# 0251 — Set, Clear, Toggle Bit

On bit position 1: set it on 0 (->2), clear it on 2 (->0), toggle it on 0 (->2), printing `2 0 2`. In cpp the shift mask `1 << pos` combines with `|`, `& ~`, and `^`.

## Run

    make && ./setcleartogglebit
