# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A small iterator exposes `hasNext()`/`next()` over an inclusive integer range.

## Run

    make && ./iteratorpattern
