# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The `Shape` holds a `unique_ptr<Color>` implementor, decoupling shape from color.

## Run

    make && ./bridge
