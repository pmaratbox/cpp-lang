# 0214 — Visitor

Use a visitor to sum the values of a small node tree with leaves 1, 2, 3, printing `6`. Double dispatch via `accept(Visitor&)` lets the visitor accumulate each leaf value.

## Run

    make && ./visitor
