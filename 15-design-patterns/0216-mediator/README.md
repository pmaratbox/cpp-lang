# 0216 — Mediator

Have colleague A send "hi" through a mediator to colleague B, which prints `B got: hi`. Colleagues only know the `Mediator`, which routes the message to the other colleague.

## Run

    make && ./mediator
