# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. In C++ the factory returns a `std::unique_ptr<Shape>` to a derived type chosen at runtime.

## Run

    make && ./factorymethod
