# 0203 — Prototype (Clone)

Clone a prototype object holding 1, mutate the clone to 2, and show the original is unchanged, printing `1 2`. A `clone()` member returns a fresh `unique_ptr` copy so mutating it leaves the original intact.

## Run

    make && ./prototype
