# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Interchangeable strategies are held in a `std::function<int(int,int)>` and swapped at runtime.

## Run

    make && ./strategy
