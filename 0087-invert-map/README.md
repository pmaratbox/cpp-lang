# 0087 — Invert a Map

Invert the map `{a: 1, b: 2, c: 3}` (swap keys and values) and print the result sorted by the new key: `1:a 2:b 3:c`. Inserting each value as a key into a second `std::map` swaps the pairs and keeps them sorted by the new key.

## Run

    make && ./invertmap
