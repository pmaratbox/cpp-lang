# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. In C++ `std::getline` over an `std::ifstream` reads one line at a time until EOF.

## Run

    make && ./filelines
