# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. Build the compact string by hand to avoid stdlib spacing.

## Run

    make && ./jsonstringify
