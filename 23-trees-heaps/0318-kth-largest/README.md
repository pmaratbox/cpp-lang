# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. A size-2 `std::priority_queue` with `std::greater` keeps a min-heap of the largest seen.

## Run

    make && ./kthlargest
