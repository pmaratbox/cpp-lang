# 0320 — Fenwick Tree Prefix Sum

Build a Fenwick (BIT) tree over [1,2,3,4,5] and query the prefix sum of the first 4 elements, printing `10`. Low-bit `i & -i` stepping over a 1-indexed `std::vector`.

## Run

    make && ./fenwicktree
