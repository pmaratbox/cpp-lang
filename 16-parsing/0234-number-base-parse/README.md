# 0234 — Parse Number Bases

Parse "ff" as hexadecimal (255) and "101" as binary (5), printing `255 5`. `std::stol`'s base argument converts each digit string directly.

## Run

    make && ./numberbaseparse
