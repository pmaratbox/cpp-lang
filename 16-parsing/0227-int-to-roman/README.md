# 0227 — Integer to Roman

Format the integer 14 as the Roman numeral `XIV`. A `std::array` of value/symbol pairs and structured bindings drive the greedy subtraction.

## Run

    make && ./inttoroman
