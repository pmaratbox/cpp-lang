# 0221 — Tokenizer

Tokenize the expression "1 + 2" into token types, printing `NUM PLUS NUM`. In C++ we scan with a range over the string and classify with `<cctype>`.

## Run

    make && ./tokenizer
