# 0222 — Bracket Matching

Check that mixed brackets are balanced: "([{}])" (yes) and "([)]" (no), printing `yes no`. A `std::stack<char>` makes the push/match logic concise.

## Run

    make && ./bracketmatching
