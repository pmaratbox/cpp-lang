# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. A single pass with an `in_word` flag and `<cctype>` whitespace tests yields all three counts.

## Run

    make && ./wccounts
