# 0223 — RPN Evaluator

Evaluate the postfix expression "3 4 + 5 *" and print `35`. A `std::stack<long>` fed by a `std::istringstream` makes the stack machine idiomatic.

## Run

    make && ./rpneval
