# 0224 — Shunting Yard

Convert the infix expression "3 + 4 * 2" to postfix `3 4 2 * +`. We use a `std::stack<std::string>` for operators and a `std::vector` for the RPN output.

## Run

    make && ./shuntingyard
