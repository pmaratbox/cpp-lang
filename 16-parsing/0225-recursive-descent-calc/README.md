# 0225 — Recursive Descent Calculator

Parse and evaluate "2+3*4" honoring precedence, printing `14`. A small `Parser` struct holding the string and a cursor expresses the grammar recursively.

## Run

    make && ./recursivedescentcalc
