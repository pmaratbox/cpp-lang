# 0116 — Function Pipeline

Compose inc, double, and negate into a left-to-right pipeline and apply it to 3: ((3+1)*2) negated = `-8`. The `pipe` helper nests the calls so functions run in the order given.

## Run

    make && ./pipeline
