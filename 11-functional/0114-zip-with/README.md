# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. We walk both vectors by index, add each pair, and emit the sums space-separated.

## Run

    make && ./with
