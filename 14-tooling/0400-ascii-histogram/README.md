# 0400 — ASCII Histogram

Render a histogram of [3,1,2] as rows of "#" characters, one bar per line. Each value drives a `std::string(n, '#')` printed on its own line.

## Run

    make && ./asciihistogram
