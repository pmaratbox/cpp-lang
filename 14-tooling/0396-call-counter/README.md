# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A lambda closing over a counter wraps the callable and bumps it on each invocation.

## Run

    make && ./callcounter
