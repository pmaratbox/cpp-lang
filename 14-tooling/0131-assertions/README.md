# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. In C++ a tiny helper that throws `std::logic_error` on failure keeps assertions explicit and runtime-checked.

## Run

    make && ./assertions
