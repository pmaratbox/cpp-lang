# 0399 — Progress Bar

Render a width-10 progress bar at 40% completion, printing `[####------]`. `std::string(n, ch)` builds the filled and empty runs from the rounded fill count.

## Run

    make && ./progressbar
