# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. Casting `'A'` to `int` shows its code; casting `66` to `char` shows the character — the stream picks the right formatting per type.

## Run

    make && ./charcodes
