# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. A `std::map` lookup yields the next state each step.

## Run

    make && ./markovdeterministic
