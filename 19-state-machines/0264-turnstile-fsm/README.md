# 0264 — Turnstile FSM

Drive a turnstile (locked/unlocked) with events coin, push, push and print the resulting states `unlocked locked locked`. A simple if-chain encodes the two transitions.

## Run

    make && ./turnstilefsm
