# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. The DFA state counts matched pattern characters, falling back to 1 when a stray 'a' restarts the match.

## Run

    make && ./stringmatchdfa
