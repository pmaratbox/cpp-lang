# 0270 — Elevator FSM

Move an elevator from floor 0 up to floor 2 then back to 0, printing every floor it stops at `0 1 2 1 0`. A loop steps one floor toward each target, recording each floor in a `std::vector`.

## Run

    make && ./elevatorfsm
