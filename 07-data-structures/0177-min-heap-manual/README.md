# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An array-backed `std::vector` stores the heap while sift-up and sift-down keep the min invariant.

## Run

    make && ./heapmanual
