# 0179 — Union-Find

Union (0,1) and (2,3), then query connectivity of (0,1)=yes and (0,2)=no, printing `yes no`. A disjoint-set vector with path-halving in `find` keeps lookups near constant time.

## Run

    make && ./find
