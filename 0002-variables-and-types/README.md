# 0002 — Variables & basic types

Declare an `int`, a `double`, a `std::string`, and a `bool`, then stream
each to `std::cout`. `std::boolalpha` is a stream manipulator that switches
bool formatting from the default `1`/`0` to `true`/`false`.

## Run

    make && ./variables
