# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. `std::stack<char>` pushes on `(` and pops on `)`; an empty stack at the end means the brackets are balanced.

## Run

    make && ./balanced
