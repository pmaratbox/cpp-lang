# 0557 — Transpose

Transpose the 2x3 integer matrix `[[1,2,3],[4,5,6]]` using Eigen's `MatrixXi` and its `transpose()` method, producing a 3x2 matrix. The transpose is computed by the library and each row is printed as space-joined integers.

## Run

    make && ./transpose
