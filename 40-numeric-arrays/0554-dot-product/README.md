# 0554 — Dot product

Compute the dot product of two integer vectors with the Eigen linear-algebra library. Building `Eigen::VectorXi` vectors `[1,2,3]` and `[4,5,6]`, `a.dot(b)` returns the integer scalar `1*4 + 2*5 + 3*6 = 32`.

## Run

    make && ./dot-product
