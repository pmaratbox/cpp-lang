# 0556 — Matrix multiply

Multiply two 2x2 integer matrices `[[1,2],[3,4]]` and `[[5,6],[7,8]]` using Eigen's `MatrixXi` and its `*` (matrix-multiply) operator. The product is computed by the library and each row is printed as space-joined integers.

## Run

    make && ./matrix-multiply
