# 0555 — Element-wise add

Uses the Eigen linear-algebra library: two `Eigen::VectorXi` integer vectors `[1,2,3]` and `[10,20,30]` are combined with the element-wise `+` operator, and the resulting vector is printed as space-joined integers.

## Run

    make && ./elementwise-add
