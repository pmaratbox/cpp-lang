# 0553 — Vector sum

Build the integer vector `[1, 2, 3, 4]` with Eigen's `VectorXi` and reduce it with the library's `.sum()` operation, which adds every element. The total `10` is printed as a plain integer.

## Run

    make && ./vector-sum
