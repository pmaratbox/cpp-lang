# 0558 — Scalar multiply

Using the Eigen linear-algebra library: build an integer matrix `Eigen::MatrixXi` and scale it with `scalar * a`, an element-wise scalar multiplication that returns a new matrix. Each row is printed as space-joined integers.

## Run

    make && ./scalar-multiply
