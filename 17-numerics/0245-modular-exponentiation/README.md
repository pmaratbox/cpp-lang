# 0245 — Modular Exponentiation

Compute 3^13 mod 7 by fast modular exponentiation, printing `3`. Square-and-multiply runs in a tight loop over the exponent bits.

## Run

    make && ./modularexponentiation
