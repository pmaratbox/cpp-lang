# 0237 — Big Number Multiplication

Multiply 123 by 456 with schoolbook long multiplication on digit strings, printing `56088`. A `std::vector<int>` holds positional products before flattening to a string.

## Run

    make && ./bignummultiply
