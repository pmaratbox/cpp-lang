# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. In C++ an `std::array` of names maps the formula's 0=Saturday result to a label.

## Run

    make && ./ofweekzeller
