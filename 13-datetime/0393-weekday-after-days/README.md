# 0393 — Weekday After N Days

Given 2000-01-01 is a Saturday, compute the weekday 3 days later, printing `Tuesday`. A constexpr array of names indexed by modular arithmetic is the idiomatic C++ approach.

## Run

    make && ./weekdayafterdays
