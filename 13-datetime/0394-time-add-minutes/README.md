# 0394 — Add Minutes to Time

Add 90 minutes to 10:45 and print the resulting 24-hour time `12:15`. Stream manipulators `std::setw`/`std::setfill` give idiomatic zero-padding in C++.

## Run

    make && ./timeaddminutes
