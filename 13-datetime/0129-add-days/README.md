# 0129 — Add Days to a Date

Add 40 days to 2000-01-01 and print the result as `2000-02-10`. In C++ convert to a serial day count, add 40, then `civil_from_days` back and format with `std::setw`/`setfill`.

## Run

    make && ./days
