# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. In C++ a small `is_leap` helper with the divisibility rule keeps the logic readable.

## Run

    make && ./year
