# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. `std::rotate` makes the element at `begin()+2` the new first, rotating the range left by two in place.

## Run

    make && ./rotate
