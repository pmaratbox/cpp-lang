# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. Constructing a string from reverse iterators (`rbegin()`/`rend()`) makes the mirror image to compare.

## Run

    make && ./palindrome
