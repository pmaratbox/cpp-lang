# 0298 — Bellman-Ford

On the digraph with a negative edge 0->1(1),1->2(-2),0->2(4), print the shortest distances from node 0 `0 1 -1`. Structured bindings over a `std::tuple` edge list make the V-1 relaxation passes read cleanly.

## Run

    make && ./bellmanford
